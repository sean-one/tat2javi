import React from 'react';

// styling
import './booking.css';

const Booking = (props) => {
    return (
        <div className='appointment'>
            this is the booking section
        </div>
    );
}

export default Booking;