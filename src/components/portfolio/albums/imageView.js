import React from 'react';

const imageView = (props) => {
    // const images = {
    //     01: ''
    // }
    return (
        <div>
            
        </div>
    );
}

export default imageView;
